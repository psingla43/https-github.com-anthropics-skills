import XCTest

final class FeatureFlowTests: BaseUITestCase {

    func test_example_flow() {
        // Arrange
        // let screen = SomeScreen(app: app)

        // Act
        app.handleSystemAlertsIfPresent()
        // screen.assertLoaded()
        // screen.doPrimaryAction()

        // Assert
        // XCTAssertTrue(app.staticTexts["some.anchor"].waitForVisible(timeout: 10))
    }
}
