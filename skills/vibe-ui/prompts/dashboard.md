Requirements:
- Follow DESIGN.md strictly for color, typography, density, radius, shadow, and spacing.
- Give major regions stable `data-od-id` values such as `dashboard-shell`, `metrics`, `primary-workflow`, and `activity`.
- Create a functional dashboard layout with sidebar or topbar, content area, metric cards, table/list, filters, and primary actions.
- Keep information density aligned with the selected style.
- Use realistic empty, loading, and active states where the project pattern supports them.
- Do not overuse shadows, gradients, or borders.
- Make components reusable and responsive.
- Do not invent metrics or growth claims; use neutral labels when data is unavailable.
- P0 self-check before finishing: controls have stable dimensions, table/list rows scan cleanly, mobile layout remains usable, and no marketing hero-scale type appears inside dashboard panels.
