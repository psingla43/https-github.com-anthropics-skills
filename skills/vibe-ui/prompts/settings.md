Requirements:
- Follow DESIGN.md strictly for form controls, surfaces, section rhythm, and button hierarchy.
- Include grouped settings sections, toggles, selects, text inputs, save/cancel actions, and status feedback.
- Keep density aligned with the selected style.
- Use existing project components and validation patterns.
- Avoid unsupported shadows, gradients, and arbitrary hardcoded colors.
