Requirements:
- Follow DESIGN.md strictly for navigation, typography scale, code blocks, links, and content rhythm.
- Give shell, sidebar, article, examples, and footer/navigation regions stable `data-od-id` values.
- Include sidebar navigation, article body, search or quick links, code examples, callouts, and next/previous navigation.
- Keep documentation dense enough for scanning but not visually noisy.
- Do not add arbitrary gradients or card treatments unless DESIGN.md supports them.
- Use semantic HTML and accessible landmarks.
- Avoid placeholder docs copy; write concrete headings, parameters, and examples.
- P0 self-check before finishing: headings are semantic, code blocks are readable on mobile, links have visible focus states, and callouts use DESIGN.md tokens.
