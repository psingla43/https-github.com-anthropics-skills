{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Go: Current File",
      "type": "go",
      "request": "launch",
      "program": "${file}",
      "cwd": "${workspaceFolder}"
    }
  ]
}