{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "compose: up",
      "type": "shell",
      "command": "docker",
      "args": ["compose", "-f", "${workspaceFolder}/docker-compose.yml", "up"],
      "isBackground": true,
      "problemMatcher": [],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    },
    {
      "label": "compose: down",
      "type": "shell",
      "command": "docker",
      "args": ["compose", "-f", "${workspaceFolder}/docker-compose.yml", "down"],
      "problemMatcher": []
    }
  ]
}